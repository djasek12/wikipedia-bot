#!/usr/bin/python

import wikipedia as wiki
import webutils







def traverse(src, dst):
	frontier = [] # heap containing the next locations to go to
	marked = set() # contains all visited links

	for links in links:
		# heap.heappush(Jaccard(dst, link), link)

	
		
